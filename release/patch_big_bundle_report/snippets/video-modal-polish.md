### Video Modal Polish
- Added vertical detection in `openVideo(url)` via `isVerticalVideoUrl(url)`.
- Toggles `.video-inner.vertical` and `.video-frame.vertical`.
- Keeps close behavior: cross button, backdrop, `Esc`.
