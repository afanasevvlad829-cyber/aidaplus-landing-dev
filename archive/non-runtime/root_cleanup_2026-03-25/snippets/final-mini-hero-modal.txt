/* Hero compacting + compact modal containment */
.compact-mode .hero-shell{ min-height:540px; }
.hero-shell{ padding:22px 22px 0; min-height:540px; }
.hero-booking-card{ margin-bottom:-32px; transform:translateY(-14px); }
.hero-booking-card:hover{ transform:translateY(-16px); }
.overlay.section-modal-compact{ padding:84px 0 0 20px; }
.overlay.section-modal-compact .section-modal-card{
  width:min(52vw,680px);
  max-width:calc(100vw - 420px);
  max-height:min(60vh,520px);
}
