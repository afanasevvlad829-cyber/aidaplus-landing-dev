(function () {
  "use strict";

  window.AC_APP = window.AC_APP || {};
  if (window.AC_APP.booted) return;
  window.AC_APP.booted = true;

  // Runtime bootstrap remains in features/events.js (legacy-safe path).
})();
