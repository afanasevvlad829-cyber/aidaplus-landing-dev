# Local WIP Archive (2026-03-25)

This folder contains local/untracked working copies moved out of repository root during cleanup.

Archived directories:
- `gpt-publish-temp` (from `.tmp/worktrees/gpt-publish-temp`)
- `aidaplus-step2d` (from repo root)

Purpose:
- keep `dev` root clean
- preserve local artifacts for later inspection
- avoid accidental use of parallel local copies in active development
