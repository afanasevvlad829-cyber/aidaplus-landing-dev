diff --git a/dist/index.htm b/dist/index.htm
index bc65f4a..5a9e6af 100644
--- a/dist/index.htm
+++ b/dist/index.htm
@@ -1420,26 +1420,26 @@
       box-shadow:0 8px 14px rgb(17 24 39 / 10%);
     }
 
-    #desktopShiftOptions .shift-option{
+    #desktop-shift-options .shift-option{
       padding:16px 16px 14px;
       gap:12px;
       border-radius:18px;
       margin-bottom:12px;
     }
 
-    #desktopShiftOptions .shift-option strong{
+    #desktop-shift-options .shift-option strong{
       font-size:19px;
       line-height:1.2;
       letter-spacing:-.01em;
     }
 
-    #desktopShiftOptions .shift-option-head small{
+    #desktop-shift-options .shift-option-head small{
       font-size:14px;
       line-height:1.25;
       color:#5f6779;
     }
 
-    #desktopShiftOptions .shift-option-action{
+    #desktop-shift-options .shift-option-action{
       min-height:36px;
       font-size:13px;
       padding:0 12px;
@@ -6213,7 +6213,7 @@
               </div>
               <div class="guided-inline-hint" id="desktopInlineHint"></div>
               <div class="shift-list" id="desktopShiftList">
-                <div id="desktopShiftOptions"></div>
+                <div id="desktop-shift-options"></div>
               </div>
               <div class="shift-list-veil">Сначала выберите возраст, чтобы активировать список смен.</div>
               <button class="booking-all-shifts-link" type="button" data-action="open-all-shifts">
@@ -8845,7 +8845,7 @@
         }
       }
 
-      const shiftWrap = e.target.closest('#desktopShiftOptions, #mobileShiftOptions');
+      const shiftWrap = e.target.closest('#desktop-shift-options, #mobileShiftOptions');
       const shiftBtn = e.target.closest('button, .shift-option, .slot-card');
       if(shiftWrap && shiftBtn){
         const shiftText = (shiftBtn.textContent || '').trim().split('\n')[0];
@@ -8956,7 +8956,7 @@
       const safeView = viewKey === 'mobile' ? 'mobile' : 'desktop';
       const current = shiftOptionPanels[safeView]?.[panelType] || null;
       shiftOptionPanels[safeView][panelType] = current === shiftId ? null : shiftId;
-      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktopShiftOptions');
+      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktop-shift-options');
     }
 
     function clearShiftOptionPanels(){
@@ -10004,7 +10004,7 @@
 
     function renderAll(){
       renderShiftCards();
-      renderShiftOptions('desktopShiftOptions');
+      renderShiftOptions('desktop-shift-options');
       renderShiftOptions('mobileShiftOptions');
       renderBookingPanels();
       renderGuidedState('desktop');
@@ -10597,7 +10597,7 @@
     initHero();
     loadVideoMetaCache();
 
-    renderShiftOptions('desktopShiftOptions');
+    renderShiftOptions('desktop-shift-options');
     renderShiftOptions('mobileShiftOptions');
     renderShiftCards();
     renderMediaSections();
diff --git a/dist/index.html b/dist/index.html
index bc65f4a..5a9e6af 100644
--- a/dist/index.html
+++ b/dist/index.html
@@ -1420,26 +1420,26 @@
       box-shadow:0 8px 14px rgb(17 24 39 / 10%);
     }
 
-    #desktopShiftOptions .shift-option{
+    #desktop-shift-options .shift-option{
       padding:16px 16px 14px;
       gap:12px;
       border-radius:18px;
       margin-bottom:12px;
     }
 
-    #desktopShiftOptions .shift-option strong{
+    #desktop-shift-options .shift-option strong{
       font-size:19px;
       line-height:1.2;
       letter-spacing:-.01em;
     }
 
-    #desktopShiftOptions .shift-option-head small{
+    #desktop-shift-options .shift-option-head small{
       font-size:14px;
       line-height:1.25;
       color:#5f6779;
     }
 
-    #desktopShiftOptions .shift-option-action{
+    #desktop-shift-options .shift-option-action{
       min-height:36px;
       font-size:13px;
       padding:0 12px;
@@ -6213,7 +6213,7 @@
               </div>
               <div class="guided-inline-hint" id="desktopInlineHint"></div>
               <div class="shift-list" id="desktopShiftList">
-                <div id="desktopShiftOptions"></div>
+                <div id="desktop-shift-options"></div>
               </div>
               <div class="shift-list-veil">Сначала выберите возраст, чтобы активировать список смен.</div>
               <button class="booking-all-shifts-link" type="button" data-action="open-all-shifts">
@@ -8845,7 +8845,7 @@
         }
       }
 
-      const shiftWrap = e.target.closest('#desktopShiftOptions, #mobileShiftOptions');
+      const shiftWrap = e.target.closest('#desktop-shift-options, #mobileShiftOptions');
       const shiftBtn = e.target.closest('button, .shift-option, .slot-card');
       if(shiftWrap && shiftBtn){
         const shiftText = (shiftBtn.textContent || '').trim().split('\n')[0];
@@ -8956,7 +8956,7 @@
       const safeView = viewKey === 'mobile' ? 'mobile' : 'desktop';
       const current = shiftOptionPanels[safeView]?.[panelType] || null;
       shiftOptionPanels[safeView][panelType] = current === shiftId ? null : shiftId;
-      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktopShiftOptions');
+      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktop-shift-options');
     }
 
     function clearShiftOptionPanels(){
@@ -10004,7 +10004,7 @@
 
     function renderAll(){
       renderShiftCards();
-      renderShiftOptions('desktopShiftOptions');
+      renderShiftOptions('desktop-shift-options');
       renderShiftOptions('mobileShiftOptions');
       renderBookingPanels();
       renderGuidedState('desktop');
@@ -10597,7 +10597,7 @@
     initHero();
     loadVideoMetaCache();
 
-    renderShiftOptions('desktopShiftOptions');
+    renderShiftOptions('desktop-shift-options');
     renderShiftOptions('mobileShiftOptions');
     renderShiftCards();
     renderMediaSections();
diff --git a/gpt.html b/gpt.html
index bc65f4a..5a9e6af 100644
--- a/gpt.html
+++ b/gpt.html
@@ -1420,26 +1420,26 @@
       box-shadow:0 8px 14px rgb(17 24 39 / 10%);
     }
 
-    #desktopShiftOptions .shift-option{
+    #desktop-shift-options .shift-option{
       padding:16px 16px 14px;
       gap:12px;
       border-radius:18px;
       margin-bottom:12px;
     }
 
-    #desktopShiftOptions .shift-option strong{
+    #desktop-shift-options .shift-option strong{
       font-size:19px;
       line-height:1.2;
       letter-spacing:-.01em;
     }
 
-    #desktopShiftOptions .shift-option-head small{
+    #desktop-shift-options .shift-option-head small{
       font-size:14px;
       line-height:1.25;
       color:#5f6779;
     }
 
-    #desktopShiftOptions .shift-option-action{
+    #desktop-shift-options .shift-option-action{
       min-height:36px;
       font-size:13px;
       padding:0 12px;
@@ -6213,7 +6213,7 @@
               </div>
               <div class="guided-inline-hint" id="desktopInlineHint"></div>
               <div class="shift-list" id="desktopShiftList">
-                <div id="desktopShiftOptions"></div>
+                <div id="desktop-shift-options"></div>
               </div>
               <div class="shift-list-veil">Сначала выберите возраст, чтобы активировать список смен.</div>
               <button class="booking-all-shifts-link" type="button" data-action="open-all-shifts">
@@ -8845,7 +8845,7 @@
         }
       }
 
-      const shiftWrap = e.target.closest('#desktopShiftOptions, #mobileShiftOptions');
+      const shiftWrap = e.target.closest('#desktop-shift-options, #mobileShiftOptions');
       const shiftBtn = e.target.closest('button, .shift-option, .slot-card');
       if(shiftWrap && shiftBtn){
         const shiftText = (shiftBtn.textContent || '').trim().split('\n')[0];
@@ -8956,7 +8956,7 @@
       const safeView = viewKey === 'mobile' ? 'mobile' : 'desktop';
       const current = shiftOptionPanels[safeView]?.[panelType] || null;
       shiftOptionPanels[safeView][panelType] = current === shiftId ? null : shiftId;
-      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktopShiftOptions');
+      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktop-shift-options');
     }
 
     function clearShiftOptionPanels(){
@@ -10004,7 +10004,7 @@
 
     function renderAll(){
       renderShiftCards();
-      renderShiftOptions('desktopShiftOptions');
+      renderShiftOptions('desktop-shift-options');
       renderShiftOptions('mobileShiftOptions');
       renderBookingPanels();
       renderGuidedState('desktop');
@@ -10597,7 +10597,7 @@
     initHero();
     loadVideoMetaCache();
 
-    renderShiftOptions('desktopShiftOptions');
+    renderShiftOptions('desktop-shift-options');
     renderShiftOptions('mobileShiftOptions');
     renderShiftCards();
     renderMediaSections();
diff --git a/index.html b/index.html
index a956dac..90e3932 100644
--- a/index.html
+++ b/index.html
@@ -4658,7 +4658,7 @@
               </div>
               <div class="guided-inline-hint" id="desktopInlineHint"></div>
               <div class="shift-list" id="desktopShiftList">
-                <div id="desktopShiftOptions"></div>
+                <div id="desktop-shift-options"></div>
               </div>
               <div class="shift-list-veil">Сначала выберите возраст, чтобы активировать список смен.</div>
               <button class="booking-all-shifts-link" type="button" data-action="open-all-shifts">
@@ -7082,7 +7082,7 @@
         }
       }
 
-      const shiftWrap = e.target.closest('#desktopShiftOptions, #mobileShiftOptions');
+      const shiftWrap = e.target.closest('#desktop-shift-options, #mobileShiftOptions');
       const shiftBtn = e.target.closest('button, .shift-option, .slot-card');
       if(shiftWrap && shiftBtn){
         const shiftText = (shiftBtn.textContent || '').trim().split('\n')[0];
@@ -7193,7 +7193,7 @@
       const safeView = viewKey === 'mobile' ? 'mobile' : 'desktop';
       const current = shiftOptionPanels[safeView]?.[panelType] || null;
       shiftOptionPanels[safeView][panelType] = current === shiftId ? null : shiftId;
-      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktopShiftOptions');
+      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktop-shift-options');
     }
 
     function clearShiftOptionPanels(){
@@ -7899,7 +7899,7 @@
     }
 
     function renderAll(){
-      renderShiftOptions('desktopShiftOptions');
+      renderShiftOptions('desktop-shift-options');
       renderShiftOptions('mobileShiftOptions');
       renderBookingPanels();
       renderGuidedState('desktop');
@@ -8470,7 +8470,7 @@
     initHero();
     loadVideoMetaCache();
 
-    renderShiftOptions('desktopShiftOptions');
+    renderShiftOptions('desktop-shift-options');
     renderShiftOptions('mobileShiftOptions');
     renderShiftCards();
     renderMediaSections();
diff --git a/src/scripts/main.js b/src/scripts/main.js
index a11e39c..dfc0a41 100644
--- a/src/scripts/main.js
+++ b/src/scripts/main.js
@@ -1979,7 +1979,7 @@
         }
       }
 
-      const shiftWrap = e.target.closest('#desktopShiftOptions, #mobileShiftOptions');
+      const shiftWrap = e.target.closest('#desktop-shift-options, #mobileShiftOptions');
       const shiftBtn = e.target.closest('button, .shift-option, .slot-card');
       if(shiftWrap && shiftBtn){
         const shiftText = (shiftBtn.textContent || '').trim().split('\n')[0];
@@ -2090,7 +2090,7 @@
       const safeView = viewKey === 'mobile' ? 'mobile' : 'desktop';
       const current = shiftOptionPanels[safeView]?.[panelType] || null;
       shiftOptionPanels[safeView][panelType] = current === shiftId ? null : shiftId;
-      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktopShiftOptions');
+      renderShiftOptions(safeView === 'mobile' ? 'mobileShiftOptions' : 'desktop-shift-options');
     }
 
     function clearShiftOptionPanels(){
@@ -3138,7 +3138,7 @@
 
     function renderAll(){
       renderShiftCards();
-      renderShiftOptions('desktopShiftOptions');
+      renderShiftOptions('desktop-shift-options');
       renderShiftOptions('mobileShiftOptions');
       renderBookingPanels();
       renderGuidedState('desktop');
@@ -3731,7 +3731,7 @@
     initHero();
     loadVideoMetaCache();
 
-    renderShiftOptions('desktopShiftOptions');
+    renderShiftOptions('desktop-shift-options');
     renderShiftOptions('mobileShiftOptions');
     renderShiftCards();
     renderMediaSections();
diff --git a/src/styles/main.css b/src/styles/main.css
index f86ad16..50ae100 100644
--- a/src/styles/main.css
+++ b/src/styles/main.css
@@ -1407,26 +1407,26 @@
       box-shadow:0 8px 14px rgb(17 24 39 / 10%);
     }
 
-    #desktopShiftOptions .shift-option{
+    #desktop-shift-options .shift-option{
       padding:16px 16px 14px;
       gap:12px;
       border-radius:18px;
       margin-bottom:12px;
     }
 
-    #desktopShiftOptions .shift-option strong{
+    #desktop-shift-options .shift-option strong{
       font-size:19px;
       line-height:1.2;
       letter-spacing:-.01em;
     }
 
-    #desktopShiftOptions .shift-option-head small{
+    #desktop-shift-options .shift-option-head small{
       font-size:14px;
       line-height:1.25;
       color:#5f6779;
     }
 
-    #desktopShiftOptions .shift-option-action{
+    #desktop-shift-options .shift-option-action{
       min-height:36px;
       font-size:13px;
       padding:0 12px;
